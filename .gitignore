venv/
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# ngrok binaries (dev-only tunnel tool, shouldn't be versioned)
ngrok.exe
ngrok-v3-stable-windows-amd64.zip

# env / secrets
.env

# model artifacts (large binaries, regenerated by training scripts)
models_store/
